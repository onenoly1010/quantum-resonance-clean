# Database (postgres)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/ledger_db

# JWT secret for internal endpoints
GUARDIAN_JWT_SECRET=replace_with_secret

# App configuration
ENV=development
API_HOST=0.0.0.0
API_PORT=8000

# TODO: Add any external service endpoints here (e.g., SUPABASE_URL). No secrets here.
