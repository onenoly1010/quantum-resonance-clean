version: "3.8"

services:
  quantum-resonance:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload
